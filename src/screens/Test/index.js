import React from 'react';
import { StatusBar, SafeAreaView } from 'react-native';

export default function Test() {
  return (
    <SafeAreaView>
      <StatusBar hidden />
    </SafeAreaView>
  );
}
