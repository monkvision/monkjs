import { VideoCapture } from '../../src';

describe('VideoCapture component', () => {
  // TODO : Write the tests for this component when it is finished

  it('should be defined', () => {
    expect(VideoCapture).toBeDefined();
  });
});
