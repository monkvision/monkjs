import { Styles } from '@monkvision/types';

export const styles: Styles = {
  wrapper: {
    display: 'flex',
    alignItems: 'center',
    gap: '30px',
    flexGrow: 1,
    maxHeight: '100%',
  },
};
