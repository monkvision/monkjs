describe('monk-js-sdk', () => {
  it('creates an axios server', () => false);
  it('authenticates with auth0', () => false);
  it('get one inspection', () => false);
  it('get several inspections', () => false);
  it('create a new inspection', () => false);
  it('receive all inspection results', () => false);
  it('validate/invalidate damage', () => false);
  it('add new damages', () => false);
  it('send information over the inspection', () => false);
  it('display a report of the inspection', () => false);
  it('manage use profile', () => false);
  it('logout and protect user data', () => false);
});
